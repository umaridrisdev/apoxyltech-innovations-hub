/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    // Add the R2 public URL base / custom domain here once configured.
    remotePatterns: [],
  },
};

module.exports = nextConfig;
